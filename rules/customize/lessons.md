# lessons.md — Append-only workflow-failure log

This file is yours: it lives in `rules/customize/`, so the installer never
overwrites it once it exists. Base rule files carry no `## Lessons` section
of their own (they're unconditionally overwritten on upgrade) — recurring
workflow failures for a given base rule land in that rule's section below.

Entry format: `- YYYY-MM-DD (model): symptom → root cause → adjustment made/proposed`

## decomposition

## delegation-templates

## dispatch

## judgment

## maintenance

## risk-tiers
